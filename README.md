# Yuki
A minimalist AI idea manager that limits your focus to 2 active projects while automatically summarizing and archiving the rest into an "Icebox" using your own Gemini API key.

The clients can directly install the app on their android through the apk file. 

v1.2 Discussion function added, multiple LLMs added, detailed steps structure added, local database support added.  
